services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: jdgap
      POSTGRES_PASSWORD: jdgap
      POSTGRES_DB: jdgap
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: